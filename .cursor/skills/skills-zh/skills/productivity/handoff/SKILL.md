---
name: handoff
description: 将当前对话压缩为交接文档，供另一个代理继续。
argument-hint: "下一个会话将用于什么？"
---

编写交接文档，总结当前对话，以便新代理可以继续工作。将其保存到由 `mktemp -t handoff-XXXXXX.md` 生成的路径（在写入之前读取文件）。

建议下一个会话使用的技能（如果有）。

不要重复已在其他工件（PRD、计划、ADR、issue、提交、差异）中捕获的内容。通过路径或 URL 引用它们。

如果用户传递了参数，将它们视为下一个会话将关注的内容的描述，并相应地调整文档。
